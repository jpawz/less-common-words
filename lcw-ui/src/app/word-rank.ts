export class WordRank {
    id: number;
    word: string;
    rank: number;

    constructor(id: number, word: string, rank: number) {
        this.id = id;
        this.word = word;
        this.rank = rank;
    }
}
